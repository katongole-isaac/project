import { makeStyles } from "@mui/styles";

const useMigrantComplaintStyles = makeStyles({
  Box: {
    backgroundColor: "#FAFAFA",
    height: "100vh",
  },
  reasonWrapper: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  complaintDash: {
    // backgroundColor: "#fdfcdc",
    position: 'relative',
    backgroundImage: "linear-gradient(#fefefe,#f4f4f6)",
  },
});
export default useMigrantComplaintStyles;
